{
  "name": "useless-monorepo",
  "private": "true",
  "scripts": {
    "changes:release": "changeset publish",
    "changes:release:gh": "changeset publish  --no-git-tag",
    "changes:add": "changeset add"
  },
  "devDependencies": {
    "@changesets/cli": "^2.26.2"
  }
}
